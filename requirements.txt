fastapi
uvicorn==0.30.1
pydantic
pydantic-settings
yfinance==0.2.54
pandas==2.2.2
numpy==1.26.4
ta==0.11.0
alpaca-trade-api==3.2.0
# Pinned explicitly: alpaca-trade-api needs <11, and data_deriv.py /
# deriv_engine.py talk to Deriv's websocket API directly (no client library —
# python_deriv_api was removed from here, it was never actually imported)
# so this exact version is what their live connection is built against.
# yfinance>=1.0 forces websockets>=13 (its optional live-streaming feature)
# which is a HARD import at module load and will break the live trading
# connection if it's ever allowed to upgrade this — do not bump yfinance
# past the 0.2.x series without re-pinning websockets afterward.
websockets==10.3
APScheduler==3.10.4
sqlalchemy==2.0.30
click==8.1.7
rich==13.7.1
python-dotenv==1.0.1
psycopg2-binary==2.9.9
